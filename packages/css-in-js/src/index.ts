import useClasses, { Styles } from './hooks/useClasses'
import useGlobalStyles from './hooks/useGlobalStyles'
import useSharedClasses from './hooks/useSharedClasses'
import { createStyles } from './hooks/utils'

export {
    Styles,
    useGlobalStyles,
    useClasses,
    useSharedClasses,
    createStyles,
}
